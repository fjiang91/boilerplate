//Initail State of Reducer
const initialState = {}

//Action Type

//Action Creator

//Thunk Action Creator

const dummyReducer = ( state = initialState, action ) => {
  switch(action.type){
    default:
      return state;
  }
}

export default dummyReducer;
